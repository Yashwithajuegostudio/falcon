/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  basePath: "",
  compiler: {
    styledComponents: true,
  },
};

module.exports = nextConfig;
